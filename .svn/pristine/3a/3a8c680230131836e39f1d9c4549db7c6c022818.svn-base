<ul id="brandListIdArr">
    @if($brandList)
        @foreach($brandList as $k => $v)
            <li><input type="button" value="{{ $v['name'] }}" data-brand-id="{{ $v['id'] }}" class="button123 brand @if($k == 0) a1 @endif @if($k == 0) teshu @endif"></li>
        @endforeach
    @endif
</ul>