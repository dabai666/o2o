
    @foreach($data as $k => $v)
        <div class="width box @if($v['timeStatus'] == 1) timestart @endif kill_nav" time='{{ $v['startTime'] }}' maxtime="{{ $v['maxTime'] }} " kill-id="{{ $k }}" id="kill_id_{{ $k }}">
        </div>
    @endforeach
